import compiler from './compiler/';

import {
  createComponent,
  onMount,
  onUpdate,
  reactive,
  methods,
  template,
  register
} from './core/index';

export default {
  compiler,
  createComponent,
  onMount,
  onUpdate,
  reactive,
  methods,
  template,
  register
};